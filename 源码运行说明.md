# D7 PMU IAP 工具 — 源码运行说明

> 本工具是 PySide6 写的 CAN/IAP 上位机。因为 CAN 驱动 `zlgcan.dll` 是 **32 位**，
> 而 PySide6 只有 **64 位**，所以需要 **同时装一个 64 位和一个 32 位 Python**：
> 64 位跑界面，32 位在后台驱动 CAN 盒子（自动启动，无需手动操作）。

## 一、装硬件驱动（只做一次）

按顺序运行 `驱动/` 目录里的两个安装包：

1. **ZCANPRO_Setup_V2.2.5.exe** —— 装完会得到 `C:\Program Files (x86)\ZCANPRO\zlgcan.dll`，程序默认从这里加载。
2. **USB驱动安装工具Setup(V1.40).exe** —— CAN 盒子的 USB 驱动。

装完插上 CAN 盒子，**先打开 ZCanPro 确认能识别/打开设备**，再往下走。

## 二、装两个 Python

到 https://www.python.org/downloads/windows/ 下载安装。安装时**务必勾选**
`Add python.exe to PATH` 和 `py launcher`（程序靠 py launcher 自动找 32 位）。

| Python | 下载哪个 | 装完做什么 |
|--------|----------|------------|
| 64 位（跑界面） | Windows installer (64-bit) | 见第三步装 PySide6 |
| 32 位（跑 CAN 后台） | Windows installer (**32-bit**) | 裸装即可，不用装任何包 |

> 推荐 Python 3.12 或 3.13（开发者用 3.14，新版本 PySide6 跟进可能滞后，3.12/3.13 更稳）。

验证两个都装好了（PowerShell 里运行）：
```powershell
py -3-64 --version
py -3-32 --version
```
两条都能打印版本号即 OK。若 `py -3-32` 报错，说明 32 位 Python 没装好或没装 py launcher。

## 三、装界面依赖（只在 64 位 Python 上装）

在项目根目录打开 PowerShell：
```powershell
py -3-64 -m pip install -r requirements.txt
```

## 四、运行

```powershell
py -3-64 widget.py
```

界面起来后：选 APP bin 固件 → 连接 CAN → 开始升级。

---

## 常见问题

- **报「找不到 32 位 Python」**：32 位 Python 没装，或没勾 py launcher。
  解决：装 32 位 Python，或设环境变量 `D7_PYTHON32` 指向 32 位 `python.exe` 全路径。
- **CAN 打不开 / 设备无法识别**：先在 ZCanPro 里确认盒子能打开；不行就重装 USB 驱动。
- **界面里 DLL 路径不对**：若 ZCanPro 装在非默认位置，在界面「选择 DLL」指向你的 `zlgcan.dll`。
- **pip 装 PySide6 失败**：多半是 Python 版本太新，换 3.12/3.13 重试。
